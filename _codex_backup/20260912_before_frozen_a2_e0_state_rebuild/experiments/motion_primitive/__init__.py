"""Motion-primitive trajectory HAR-CGCD experiment package.

The package initializer deliberately imports no experiment implementation;
canonical entry points load only the modules they actually use.
"""
